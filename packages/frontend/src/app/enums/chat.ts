export enum ContentType {
    AUDIO = "audio",
    TEXT = "text"
}

export enum MessageType {
    SENT = "sent",
    RECEIVED = "received"
}

export enum ChatItemPosition {
    LEFT = "left",
    RIGHT = "right"
}